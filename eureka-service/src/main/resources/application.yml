server:
  port: 9000
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9000/eureka/
    fetch-registry: false
    register-with-eureka: false
spring:
  application:
    name: eureka-service